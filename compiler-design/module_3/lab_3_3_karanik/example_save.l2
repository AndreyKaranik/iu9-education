bool[] {foo} <- int {a}, bool {abc} =
int {a}, {b} := 100;
int {a} := ({f} <- {a}, {b});
char[] {text} := "content" "123" "%BEL%" %A;
{a} := ({f} <- {a}) + ({g} <- {b}, {c});
{g} <- {a}+{b}, ({a}<{b});

{a} < 0 then
{b} := 0
else {a} == 5 then
{b} := 5
else
{b} := 10..;

return true.

int {f} <- int {a} =
return {a}.

int {g} <- int {a}, int {b} =
return {a}+{b}.